<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!--
        Android 17 (API 37) stops bundling local-network access into INTERNET.
        Everything Dozecam does — console, video, wake-on-sound — is LAN-only,
        so without this every connection is dropped as a connect timeout.
    -->
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:name=".DozecamApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dozecam">

        <!--
            The viewer is also the wake target: a sound alert's full-screen
            intent must reach it over the lock screen. singleTask so an alert
            raised while the user is in settings comes back to the cameras
            instead of resuming whatever was on top; configChanges so rotating
            re-lays out the grid without tearing every stream down.

            Deliberately no showWhenLocked/turnScreenOn here. This activity is
            exported (it is the launcher), and those attributes would let any
            app on the device throw the live nursery onto the lock screen. They
            are set at runtime instead, only for an intent carrying this
            process's alert token — see MainActivity.alertIntent.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:exported="false" />

        <!-- Dismissing the alert is an acknowledgement; it silences the alarm. -->
        <receiver
            android:name=".monitoring.AlertDismissReceiver"
            android:exported="false" />

        <service
            android:name=".monitoring.MonitoringService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
    </application>

</manifest>
